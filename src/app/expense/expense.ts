/*DOCU: get the property and data type in the web api. */

export interface Expense {
    id: number,
    name: string,
    category: string,
    description: string,
    price: number,
    createdAt: string,
    updatedAt: string
}
